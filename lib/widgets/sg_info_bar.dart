import 'package:flutter/material.dart';

class SGInfoBar extends StatelessWidget {
  const SGInfoBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // Height of the gameInfo bar.
      height: 200,
      color: Colors.transparent,

      // Children to display QUESTION, LIVES, REWARDS:
      // Coming Soon
    );
  }
}